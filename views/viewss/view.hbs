<!-- view.hbs -->
<!<h1>{{title}}</h1>

<!-- Add links to CRUD operations -->
<a href="viewadd" class="btn btn-info">Add a new project</a>

<!-- To Show data -->
<table class="table table-striped table-hover">
  <thead>
    <th>FirstName</th>
    <th>LastName</th>
    <th>Username</th>
    <th>Password</th>
  </thead>
  <tbody>
   {{#each dataset}}
    <tr>
      <td>{{this.firstname}}</Td>
      <td>{{this.lastname}}</Td>
      <td>{{this.username}}</Td>
      <td>{{this.password}}</Td>
    </tr>
  {{/each}}
  </tbody>
  
</table>
